# Handoff — 决策拍板 + 0.1.5 版本计划 → 下一会话：**主程序席驱动开发实施**

> 日期：2026-09-08 ｜ 本会话：决策拍板会（决策包 A–E）+ 三席落地（需求文档/架构复审/UI 原型）+ **GitHub v0.1.5 版本计划与 issue 体系**（**零代码实施**）
> 下一会话焦点：**按 v0.1.5 的 13 张单实施开发**（主程序席主导，协调子 agent 并行）

---

## 一、本会话交付概览

| # | 产出 | 路径 | 规模 |
|---|---|---|---|
| 1 | **决策拍板会纪要**（DR-031） | `docs/discussion/2026-09-08-decision-session-minutes.md` | 743 行 / 15 节 |
| 2 | **v1.0 需求文档** | `docs/discussion/2026-09-08-v1-requirements.md` | 1141 行 / 11 模块 / 54 功能点 |
| 3 | **架构复审 v1.2** | `docs/discussion/2026-09-08-architecture-review-v12.md` | 525 行 / ADR-0011~0016 建议 |
| 4 | **UI 原型 v1** | `docs/discussion/prototype-v1-dashboard.html` + `-notes.md` | 1329 + 121 行 |
| 5 | **真源回写** | `docs/gdd/gdd.md` §2/§5.2/§6.1–6.4/§7/§8.1–8.2/§9/§11/§16–18；`decision-log.md` **DR-031**；`README.md` 索引 | — |
| 6 | **GitHub 版本计划** | milestone **v0.1.5**（13 单 #67+#71–#82）+ v0.2 依赖声明 + #28 降 p2 | — |

**纪律**：**未写任何产品代码、未建/改 ADR 文件**；issue 已建（本轮经制作人授权）。

---

## 二、v0.1.5 版本计划（**实施入口**）

**目标**：DR-031 的 v1.0 必落项——数值地基 + 核心修复 + 架构清理（**零新玩法**）。
**不含**：v0.2 玩法扩容（19 单 #26–#45，见 v0.2 milestone）。

### 2.1 十三张单与依赖拓扑

```
#71 批0 数值数据化+死键+红线3全量（零行为变更）★一切前提
 ├─→ #72 1a 收入口径改占槽（周报双侧+破产步序+账期契约+确定性修复）★
 │    ├─→ #75 1d 事件闸（C2）
 │    ├─→ #78 1g 呈现层（显示分级+净流入预告+命名仪式落点）
 │    └─→ #80 2 任务池参数联标（C3）
 ├─→ #73 1b Σeff+多人上桌+max_staff ★
 │    ├─→ #76 1e RP 供给标定+翻雾供给源 ★
 │    │    ├─→ #77 1f 竞对死表重标+竞对条修复（还依赖 #80）
 │    │    ├─→ #81 3 科技树口径收口+域计数修复
 │    │    └─→ #82 3.5 短单局收尾（还依赖 #80）
 │    └─→ #79 1h 饱和护栏+θ/k 通路
 ├─→ #74 1c 买卡命令+卡时周预算（与 #72 同批）
 └─→ #67 bug 暂停菜单面板缺失（独立，可随时插做）
```

**★ = p0-critical-path**（#71 / #72 / #73 / #76 / #80 五张，顺序不可乱）。

### 2.2 建议实施顺序（7 个 PR 批次）

| 批次 | 单 | 说明 |
|---|---|---|
| **P1** | #71 | 零行为变更，先跑基线 `verify.sh` 存哈希，改完对比 |
| **P2** | #72 + #74 | 收入口径与算力口径同批（两者都动 `economy.json` 与周结管线） |
| **P3** | #73 | Σeff + max_staff（**是 #76/#77 的硬前提**） |
| **P4** | #75 + #76 + #80 | 事件闸 → 供给标定 → 任务池参数（三者互为输入，**建议同会话串行**） |
| **P5** | #77 + #81 | 竞对重标 + 科技树口径（等 P4 数值收敛） |
| **P6** | #78 + #79 + #82 | 呈现层 + 饱和护栏 + 短单局收尾 |
| **P7** | #67 | bug 修复（任意时点插入） |

---

## 三、必须遵守的关键裁决（实施时的硬约束）

| # | 约束 | 出处 |
|---|---|---|
| 1 | **短单局体验**：一局 160 周 ≈27 分钟；**W54 封顶 → 自由期三线接管**；L4 标到 ~95 分（约束 [93,98]，**禁超玩家**） | 纪要 §十二 / DR-031/E5 |
| 2 | **RP 供给真源**：任务 `rp_output` 唯一来源；**目标供给 P50 ∈[4910,6810)**；翻雾供给源改 `cum_influence` | 纪要 §2.9 |
| 3 | **Σrp_cost 维持 14210**（撤销调表）；红线带以 DR-027① 为准 | 纪要 A2 |
| 4 | **双重计费唯一解**：`base.cost` 即全程卡时费 → 卡时只作门槛/占用，**不得再过账 money** | D-9 |
| 5 | **卡时 = 每周预算**（非卡池扣减；璞石 60 > tier1 容量 40 会死锁） | D-10 |
| 6 | **存档零迁移**：`staff.assigned` 保持 `staff→slot`（**禁改 slot→数组**）；事件冷却新增 `cooldowns` 键（不改 `fired`） | P2 / 架构复审 §2.2 |
| 7 | **账期契约**：非周结过账计入当前未结算周；破产判定在收支步之后 | R1 / Q-R1 |
| 8 | **确定性真源**：RNG 消费点回到 3 处（清理第 4 点）；`restore()` 恢复 `_income_roll_seed` | R2 |
| 9 | **红线 3**：数值禁硬编码（**19 处违规**，#71 全量清理） | X5 |
| 10 | **不重开已裁 DR**；复议项（θ/k 阶段化、V6 占比、18 节点扩树、N6 双签）**未齐材料不动工** | 纪要 §五 |

---

## 四、本会话自我纠错留痕（供下会话警惕）

| # | 错误/风险 | 纠正 |
|---|---|---|
| 1 | A1/A2 全链建立在"12.3k 供给锚"上 | **该锚在代码中无产出通路**（对抗评审 B-1）→ 撤销调表，改为标定任务池 `rp_output` |
| 2 | "18 节点绝对口径不可行" | 判据写反（应为 `C(10) > 供给锚`）→ 改为"可行但占比降到 50%" |
| 3 | C3 只算"6 笔课题达标" | 漏算教学链占槽 7 周 → 课题改 ~60k/4 周 |
| 4 | C1 只写"关脉冲+修裂缝" | 漏破产步序（任务收入在判定之后到账） |
| 5 | 漏裁 C-B2（RP=影响力同一池） | 已补裁 |
| 6 | 纪要称"员工快照缺 research" | **不准确**（活快照已含，`:13–25` 是死代码） |
| 7 | 未发现"周报 UI 恒空""域计数恒 0""竞对条恒 0%" | 需求文档席与架构复审发现，已进 v0.1.5 |

**方法论提醒**：**数值必须回数据表实算**；**UI 数据链路必须端到端验证**（本轮三处"恒空/恒 0"都是"代码存在但链路断"）。

---

## 五、仓库与持久事实源状态

- 分支 `main`；本会话产出为 5 份新文档 + 3 份真源更新（**均已落盘，未提交**）。
- **issue 池**：v0.1.5 **13 open**（#67、#71–#82）；v0.2 **19 open**（#26–#45，其中 #28 降 p2）。
- 真源层级：`docs/gdd/gdd.md` → `decision-log.md`（DR-000~031）→ `docs/adr/` → 设计稿。
- **未创建 ADR 文件**：架构复审建议 ADR-0011~0016（算力供给模型/RP 供给真源/数值数据化/谓词单点真源/破产与结算步序契约），**由实施 PR 随行创建**。

---

## 六、下一会话唤起 Prompt（复制直接发送）

```markdown
你是《大 AI 时代》项目（开罗系 AI 实验室模拟经营，Godot 4.7.2 + GUT，仓库 /home/tangyi/dev/game/big-ai-era）的**主程序席（Lead Programmer）兼实施协调者**。

本次会话任务：**按 GitHub v0.1.5 milestone 的 13 张单实施开发**（#67、#71–#82），你负责主导并协调子 agent 并行。

## 开工四步

### 第一步：读交接与真源（按序）
1. `docs/discussion/handoffs/2026-09-08-decision-to-015-dev-handoff.md`（本交接，含依赖拓扑与硬约束）
2. `docs/discussion/2026-09-08-decision-session-minutes.md`（DR-031 裁决 + §2 实算 + §十二 短单局 + §十五 架构交叉裁决）
3. `docs/discussion/2026-09-08-v1-requirements.md`（54 功能点 + 验收点，逐单对照）
4. `docs/discussion/2026-09-08-architecture-review-v12.md`（改动面/风险/ADR 建议/PR 拆分）
5. 真源：`docs/gdd/gdd.md`、`docs/discussion/decision-log.md`（DR-000~031，禁重开）、`AGENTS.md`（红线）、`docs/standards/`（code-style/testing/scene-asset/issue-spec）
6. `gh issue list --milestone "v0.1.5"`（13 张单）

### 第二步：确认实施顺序（不得乱序）
P1 #71（零行为变更）→ P2 #72+#74 → P3 #73 → P4 #75+#76+#80 → P5 #77+#81 → P6 #78+#79+#82 → P7 #67。
**#71/#72/#73/#76/#80 是 p0-critical-path，顺序不可颠倒**（否则数值返工）。

### 第三步：逐单实施（每单闭环）
1. 读 issue（验收点即 DoD，不得自行降级）；
2. 实施（遵守红线：分层单向、核心逻辑 RefCounted、数值进 `src/data/`、autoload 禁 class_name、破坏性存档走 SaveMigrator）；
3. `bash scripts/verify.sh` **全绿**（本地必须自验证）；
4. 产出**自证表**（证据只引用不复述 + GUT passing 行）与 PR（body 引用 issue 号，走 issue-gate）；
5. 每单完成后回写 issue（勾选验收点 + 链接 PR）。

### 第四步：子 agent 协调
- **可并行**：#67（独立 bug）、#78/#79（呈现与护栏）、#81（科技树口径）——分派给子 agent，各自在独立分支实施，你负责验收合并；
- **必须串行**：#71→#72→#73→#76→#80→#77（数值链）；
- **分派纪律**：给子 agent 的 prompt 必须包含「issue 号 + 验收点 + 相关代码路径 + 红线 + verify 命令」，不得只给一句话。

## 硬约束（违反即打回）
1. **无 issue 不合码**（PR body 必须引用 issue）；
2. **verify.sh 全绿才可汇报完成**；
3. **短单局体验**：一局 160 周 ≈27 分钟，W54 封顶后三线接管；
4. **RP 供给带 [4910,6810)**；Σrp_cost 维持 14210（禁调表）；
5. **卡时 = 每周预算**；`base.cost` 即全程卡时费（禁双重计费）；
6. **存档零迁移**（`staff.assigned` 保持 staff→slot）；
7. **账期契约 + 破产判定在收支后**；RNG 消费点回到 3 处；
8. **不重开已裁 DR**；复议项（θ/k 阶段化、V6 占比、18 节点扩树、N6 双签）未齐材料不动工；
9. 数值全 `~` 占位进数据表；一切结论给证据（GDD 行 / DR 号 / 代码路径 / issue 号）。

## 产出
- 每个 PR：代码 + 测试 + 自证表 + issue 勾选；
- 阶段汇报：每完成一个 P 批次，给「本批单号 / verify 结果 / 断言新增数 / 风险与偏差 / 下一批」四行；
- 遇阻塞：标 `blocked-human` 并写清「阻塞条件 + 已尝试 + 需要的裁决」，不得自行绕过。

请立刻开始：读交接 → 确认顺序 → 从 #71 开工。
```

---

## 七、附：本会话的实算基线（实施时对照）

| 项 | 值 | 出处 |
|---|---|---|
| Σrp_cost / Σtb | 14210 / 1.4000 | `techs.json` 实算 |
| V6 点亮（目标带供给 6–7k） | P10/P50/P90 = 6/7/7 | 纪要 §2.9 |
| 饱和阈值 / 时点 | A=154.74 / **W54** | 纪要 §2.4 |
| 满配 A / score | 213.4 / 99.99 | 同上 |
| 现状周收入（待退役） | 63.5k/周 | 纪要 §2.5 |
| 目标 gate | 250k @ lab 24 周（含教学链占槽 7 周） | 纪要 §2.5 |
| 卡时费恒等式 | 9000=6×10×150；117000=30×26×150 | D-9 |
| 单局时长 | 160 周 × 10s = 1600s ≈ 26.7 min | `clock.json` |
